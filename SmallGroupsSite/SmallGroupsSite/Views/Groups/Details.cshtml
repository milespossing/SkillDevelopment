@model SmallGroupsSite.Models.Group

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Group</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>

<div>
    <table>
        <thead>
        <tr><td><h2>Leaders</h2></td></tr>
        </thead>
        <tbody>
        @foreach (Person p in Model.GroupMemberships
            .Where(gm => gm.MembershipType == MembershipType.Leader)
            .Select(gm => gm.Person))
        {
            <tr>
                <td>
                    @p.FullName
                </td>
            </tr>
        }
        </tbody>
    </table>
    <table>
        <thead>
        <tr><td><h2>Members</h2></td></tr>
        </thead>
        <tbody>
        @foreach (Person p in Model.GroupMemberships
            .Where(gm => gm.MembershipType == MembershipType.Member)
            .Select(gm => gm.Person))
        {
            <tr>
                <td>
                    @p.FullName
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
